volatility: Volatility analysis
===============================

.. raw:: html

   <div class="tabbed">
     <ul class="nav nav-tabs">
       <li class="active"><a data-toggle="tab" href="#cli">Command line interface</a></li>
       <li><a data-toggle="tab" href="#api">Artifact API</a></li>
     </ul>
     <div class="tab-content">
       <div id="cli" class="tab-pane fade in active">
         <pre>Usage: qiime longitudinal volatility [OPTIONS]

     Plot control chart of a single dependent variable, "metric", across
     multiple groups contained in sample metadata column "group_column".

   Options:
     --m-metadata-file MULTIPLE PATH
                                     Metadata file or artifact viewable as
                                     metadata. This option may be supplied
                                     multiple times to merge metadata. Sample
                                     metadata file containing
                                     individual_id_column.  [required]
     --p-group-column TEXT           Metadata column on which to separate groups
                                     for comparison  [required]
     --p-metric TEXT                 Numerical metadata or artifact column to
                                     test.  [required]
     --p-state-column TEXT           Metadata column containing state (e.g.,
                                     Time) across which samples are paired.
                                     [required]
     --p-individual-id-column TEXT   Metadata column containing IDs for
                                     individual subjects.  [required]
     --i-table ARTIFACT PATH FeatureTable[RelativeFrequency]
                                     Feature table to optionally use for paired
                                     comparisons.  [optional]
     --p-palette [terrain|rainbow|Pastel1|tab20c|magma|Accent|plasma|tab20|viridis|Dark2|inferno|Set2|Set3|Set1|tab10|Pastel2|Paired|tab20b]
                                     Color palette to use for generating
                                     boxplots.  [default: Set1]
     --p-ci FLOAT                    Size of the confidence interval to plot on
                                     control chart.  [default: 95]
     --p-plot-control-limits / --p-no-plot-control-limits
                                     Plot global mean and control limits (2X and
                                     3X standard deviations).  [default: True]
     --p-xtick-interval INTEGER      Interval between major tick marks on x axis.
                                     Defaults to 1, or autoscales to show up to
                                     30 ticks if data contain more than 30 x-axis
                                     values.  [optional]
     --p-yscale [logit|symlog|log|linear]
                                     y-axis scaling strategy to apply.  [default:
                                     linear]
     --p-spaghetti [no|yes|mean]     Co-plot spaghetti plot of per-individual
                                     trajectories. If replicates exist for an
                                     individual at the same state, "mean" will
                                     plot the mean of replicates; "yes" will plot
                                     all replicates on the same line, creating
                                     jagged spaghetti.  [default: no]
     --o-visualization VISUALIZATION PATH
                                     [required if not passing --output-dir]
     --output-dir DIRECTORY          Output unspecified results to a directory
     --cmd-config PATH               Use config file for command options
     --verbose                       Display verbose output to stdout and/or
                                     stderr during execution of this action.
                                     [default: False]
     --quiet                         Silence output if execution is successful
                                     (silence is golden).  [default: False]
     --help                          Show this message and exit.</pre>
       </div>
       <div id="api" class="tab-pane fade">
         <pre>Volatility analysis

   Plot control chart of a single dependent variable, "metric", across
   multiple groups contained in sample metadata column "group_column".

   Parameters
   ----------
   table : FeatureTable[RelativeFrequency], optional
       Feature table to optionally use for paired comparisons.
   metadata : Metadata
       Sample metadata file containing individual_id_column.
   group_column : Str
       Metadata column on which to separate groups for comparison
   metric : Str
       Numerical metadata or artifact column to test.
   state_column : Str
       Metadata column containing state (e.g., Time) across which samples are
       paired.
   individual_id_column : Str
       Metadata column containing IDs for individual subjects.
   palette : Str % Choices({'Accent', 'Dark2', 'Paired', 'Pastel1', 'Pastel2', 'Set1', 'Set2', 'Set3', 'inferno', 'magma', 'plasma', 'rainbow', 'tab10', 'tab20', 'tab20b', 'tab20c', 'terrain', 'viridis'}), optional
       Color palette to use for generating boxplots.
   ci : Float % Range(0, 100), optional
       Size of the confidence interval to plot on control chart.
   plot_control_limits : Bool, optional
       Plot global mean and control limits (2X and 3X standard deviations).
   xtick_interval : Int, optional
       Interval between major tick marks on x axis. Defaults to 1, or
       autoscales to show up to 30 ticks if data contain more than 30 x-axis
       values.
   yscale : Str % Choices({'linear', 'log', 'logit', 'symlog'}), optional
       y-axis scaling strategy to apply.
   spaghetti : Str % Choices({'mean', 'no', 'yes'}), optional
       Co-plot spaghetti plot of per-individual trajectories. If replicates
       exist for an individual at the same state, "mean" will plot the mean of
       replicates; "yes" will plot all replicates on the same line, creating
       jagged spaghetti.

   Returns
   -------
   visualization : Visualization</pre>
       </div>
     </div>
   </div>